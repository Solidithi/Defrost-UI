import { Comfortaa, Orbitron, Permanent_Marker } from "next/font/google";

export const comfortaa = Comfortaa({ subsets: ["latin"] });
export const orbitron = Orbitron({ subsets: ["latin"] });
export const permanentMarker = Permanent_Marker({
	weight: "400",
	subsets: ["latin"],
});
